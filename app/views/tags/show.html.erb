<div class="user-questions buffer-both">
  <h1><%= t('questions_for_tag') %> #<%= @tag.name %>:</h1>

  <div class="user-questions buffer-both">
    <div class="questions-list">
      <% @questions.each do |question| %>
        <%= render partial: 'questions/question', object: question %>
      <% end %>
    </div>
  </div>
</div>
